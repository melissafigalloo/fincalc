# UI Components

- All components must use shadcn/ui components.
- NO custom components should be created.
- If no suitable shadcn/ui component is installed, check the shadcn/ui registry and install an appropriate component.